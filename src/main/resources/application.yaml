spring:
  application:
    name: Prowork
  datasource:
    url: jdbc:mysql://localhost:3306/prowork?serverTimezone=UTC
    username: root
    password: 12345678@Abc
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect