spring:
  application:
    name: Prowork
  datasource:
    url: jdbc:mysql://localhost:3307/prowork_db?serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 12345678@Abc
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect